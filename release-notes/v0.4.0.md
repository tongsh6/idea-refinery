# IdeaRefinery v0.4.0

## Highlights

- Consolidate AIEF documentation into `AIEF/` and clarify sidecar navigation.
- Add project release skills and cross-platform GitFlow publishing scripts.
- Enforce release-note requirement in publish workflow (`--notes` or `--notes-file`).

## Documentation and Process

- Align GitFlow policy to `main -> feature -> develop -> release -> main`.
- Keep `release/*` branches after publish and delete merged `feature/*` branches.
- Improve contributing and index docs for clearer release checklist and paths.

## Validation

- Test suite: `python3 -m pytest` (passed)
- Build: `python3 -m build` (passed)
