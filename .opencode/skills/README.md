# Project Skills

This directory contains OpenCode-callable project skills.

Available skill names:

- `manage-gitflow-release`
- `publish-github-release`

Each skill follows OpenCode format:

- Directory: `.opencode/skills/<skill-name>/`
- File: `.opencode/skills/<skill-name>/SKILL.md`
- Frontmatter must include `name` and `description`
